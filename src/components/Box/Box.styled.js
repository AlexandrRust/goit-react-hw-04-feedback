import styled from "styled-components"

export const Box = styled.div`
    padding: 40px;
    position: absolute;
    top: 40px;
    left: 50%;
    transform: translateX(-50%);
    border-radius: 15px;
    box-shadow: 4px 5px 12px 5px rgba(0,0,0,0.58);
`;